import HomeContainer from '../containers/HomeContainer';

const Home = () => {
  return (
    <main className="min-h-screen bg-background">
      <HomeContainer />
    </main>
  );
};

export default Home;
